const userRoles = {
  admin: 'admin',
  customer: 'customer',
  staff: 'staff',
};

const userStatus = {
  confirmed: 'confirmed',
  notConfirmed: 'not-confirmed',
};

const otpStatus = {
  confirmed: 'confirmed',
  notConfirmed: 'not-confirmed',
};

module.exports = { userRoles, userStatus, otpStatus };
